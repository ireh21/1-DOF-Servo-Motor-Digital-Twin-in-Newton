# 03 Project Presentation Design Draft / 시각적 자료 적극적 활용 

이 문서는 `03_project_presentation.ipynb`를 바탕으로, 실제 `pptx`를 만들기 전에 발표 구조와 슬라이드 문안을 잡아 두기 위한 임시 설계 문서다.

목표는 아래 흐름을 20분 안에 자연스럽게 전달하는 것이다.

1. 무엇을 목표로 했는가
2. 그 목표를 위해 무엇을 배워야 했는가
3. 목표에 도달하기 위해 어떻게 구현했는가
4. 결과는 어떻게 정리해 보여 줄 것인가
5. 현재 결과를 어떻게 해석하고 어떤 한계와 개선 방향이 있는가

결과 파트는 아직 확정하지 않았으므로, 해당 슬라이드는 자리만 잡고 실제 내용은 비워 둔다.

## 전체 구성 제안

- 총 17장
- 발표 17~18분 + 질의응답 5~7분
- 결과 슬라이드 2장은 현재 비워 두고, 나중에 figure/table만 채워 넣는 구조

## 슬라이드별 설계

### 1. 제목

**제목**
- Newton 기반 단일 서보 모터 디지털 트윈

**부제**
- WMX3 실측 로그 기반 파라미터 피팅

**넣을 내용**
- Kate Ireh Hong (이름)
- 고려대학교 세종캠퍼스 전자기계융합공학과 학부 4학년 (소속)
- 2026.08.03 ~ 2026.08.31 (날짜)

**발표 포인트**
- 이 발표는 단일 모터 수준에서 Newton 기반 디지털 트윈 캘리브레이션 절차를 어떻게 만들고 검증했는지에 집중한다고 먼저 깔고 간다.

---

### 2. 발표 흐름

**제목**
- 발표 흐름

**문안**
- 프로젝트 목표와 범위
- 이번에 먼저 배워야 했던 것
- 디지털 트윈 구현 구조와 피팅 절차
- 결과 정리 방식
- 한계와 다음 개선 방향

**발표 포인트**
- 청중이 지금부터 "배경 설명", "구현 설명", "discussion"이 어떻게 이어지는지 먼저 잡게 한다.

---

### 3. 왜 이 프로젝트를 했는가

**제목**
- 프로젝트 배경

**문안**
- WMX3에서 취득한 실측 로그를 기준으로 단일 무부하 서보 모터의 응답을 Newton에서 재현하고자 했다.
- 복잡한 시스템으로 바로 가기보다, 가장 단순한 1-DOF 축에서 먼저 캘리브레이션 절차를 정리하는 것이 필요했다.
- 기존 로봇팔은 강체 중심 물리엔진 기반이었고, 대상도 대부분 강체였다.
- Newton을 사용한 이유는 deformable body를 시뮬레이션하고 시각화하는 방향으로 확장 가능성이 있기 떄문이다.

**발표 포인트**
- "왜 하필 Newton인가", "왜 하필 단일 모터부터인가"를 여기서 정리한다.

---

### 4. 이번 발표에서 다루는 목표와 범위

**제목**
- 목표와 범위

**문안**
- 목표: Command Position 입력에 대해 실제 모터와 유사한 Position, Velocity, Torque 응답을 내는 Newton 엔진 기반 모터 모델을 만든다.
- 대상: 무부하 단일 서보 모터 1축
- 방식: 실측 로그 기반 파라미터 피팅
- 모델 성격: 응답 재현용 그레이박스 근사 모델
- 포함: 지연, 제어 응답, 토크 제한, 관성, 점성/쿨롱 마찰
- 제외: 외부 부하, 감속기/링크, 다축 연동을 포함한 시스템 수준 모델링

**발표 포인트**
- "이 프로젝트가 어디까지는 했고 어디까지는 안 했다"를 분명히 해야 뒤의 limitation이 자연스럽다.

---

### 5. 먼저 이해해야 했던 것 1

**제목**
- WMX와 실측 데이터 준비

**문안**
- WMX3 제어 구조와 EtherCAT/CSP 동작 이해
- Data Log에서 사용할 신호와 의미 정리
- user unit, torque unit, 시간축 정렬 등 데이터 정합 기준 정리

**발표 포인트**
- WMX 쪽은 제어 구조 설명보다, 어떤 실측 데이터를 어떻게 해석할지에 집중한다.
- 뒤 슬라이드의 신호 정의와 입력 파형 설명으로 자연스럽게 연결한다.

---

### 6. 먼저 이해해야 했던 것 2

**제목**
- Newton 엔진과 baseline 학습

**문안**
- `franka_cube` baseline으로 Newton 실행 흐름 학습
- ModelBuilder -> Model -> State / Control -> Solver 구조 이해
- actuator 활용 방식, IK, 시각화 흐름 파악

**발표 포인트**
- Newton 자체를 깊게 설명하기보다, 이후 1-DOF 모델 구현에 필요한 구조를 익혔다는 점만 전달한다.
- 멘토가 준 baseline을 기반으로 학습했다는 맥락을 짧게 깔고 간다.

**후보 자료**
- `images/concept2.png`
- `images/4_franka_robot.png`
- 가능하면 `franka_cube` cable/deformable scene 캡처 또는 짧은 영상

---

### 7. 사용한 데이터와 입출력 정의

**제목**
- 데이터와 신호 정의

**문안**
- 입력: `Command Position`
- 출력: `Feedback Position`, `Feedback Velocity`, `Feedback Torque`
- 샘플링 주기: 1 ms
- WMX position unit: 1 U = 1 deg
- 비교 전처리: 시간축 정렬 + 단위 변환
- 단위 정합: `deg -> rad`, `deg/s -> rad/s`, `FeedbackTrq[%] -> N·m`

**발표 포인트**
- 데이터 정의가 흔들리면 피팅 전체가 흔들린다는 점을 짚는다.
- 여기서는 "무엇을 비교했는가"만 분명히 하면 된다.

---

### 8. 시험 입력 파형을 왜 나눠서 썼는가

**제목**
- 입력 파형과 역할

**문안**
- Stop: torque noise floor 확인
- Step: delay와 과도응답 확인
- Ramp: 마찰 성분 초기값 추정
- Multi-sine: `kp`, `kd`, `inertia` 초기값 추정
- Held-out S-curve: 피팅에 쓰지 않은 일반화 검증

**발표 포인트**
- 이 슬라이드는 "데이터를 많이 쓴 것"이 아니라 "역할이 다른 데이터를 나눠 쓴 것"이 핵심이다.

**슬라이드 형태 추천**
- 표 1개로 구성
- 열 예시: `입력 파형 / 관찰 목적 / 주로 보는 파라미터`

---

### 9. 전체 디지털 트윈 구조
 *이 슬라이드는 실측 커맨드 포지션 값이 모델 안에서 어떠한 흐름을 거쳐 위치속도토크 출력으로 나오는가 에 대한 설명을 한다*  
 *즉, 모터 모델링을 어떻게 했는가에 대한 설명 슬라이드*

**제목**
- 모터 모델링 구조

**문안**
```text
Command Position
    -> delay_steps
    -> PD/PID
    -> torque clamping
    -> 1-DOF inertia + viscous + coulomb friction
    -> simulated position / velocity / torque
```

- 실제 비교 대상은 위치만이 아니라 위치, 속도, 토크 3개 응답이다.
- 모델은 평균적인 축 응답 재현을 목표로 한 저차 구조다.

**발표 포인트**
- 이 발표의 기술적 중심 슬라이드다.
- 뒤의 fitting, limitation, future plan이 모두 이 구조를 기준으로 설명된다.

---

### 10. 왜 Newton 기본 actuator만으로 가지 않았는가

**제목**
- 구현 선택: actuator vs 직접 계산

**문안**
- 현재 디지털 트윈은 Newton 위에서 구동하되, 핵심 응답 요소는 사용자 코드로 직접 계산하는 방식으로 구현했다.
- 이 방식으로 delay, 토크 제한, 마찰, 응답 지연을 모델 안에서 분리해 다룰 수 있었다.
- actuator 내부에 감춰질 수 있는 제어/제한 요소를 직접 관찰하고 조정하기 쉬웠다.
- 이후에는 Newton 기본 actuator 활용을 늘리는 방향으로 확장할 수 있다.

**발표 포인트**
- 직접 계산을 택한 이유를 한계보다 장점 중심으로 설명한다.
- 현재 구현 선택과 향후 actuator 활용 확대 방향을 분리해서 말한다.
- 로봇암으로 확장시에는 고려할 요소가 많아지기에 뉴턴 actuator를 사용하는 것이 장점이 많다. 

---

### 11. 파라미터 피팅 절차

**제목**
- Parameter Fitting 절차

**문안**
1. Stop 로그로 torque noise floor 확인
2. Step 로그로 delay와 과도응답 확인
3. Ramp 로그로 마찰 초기값 추정
4. Multi-sine으로 `kp`, `kd`, `inertia` 초기값 추정
5. 전체 로그 기반 미세 조정
6. Held-out S-curve로 일반화 검증

**발표 포인트**
- "한 번에 전체 최적화"가 아니라, 파형별 역할을 활용해 점진적으로 좁혀 간 흐름을 설명

**슬라이드 형태 추천**
- 세로 순서도 또는 numbered flow

---

### 12. 결과를 어떻게 평가할 것인가

**제목**
- 평가 지표와 검증 방식

**문안**
- 기본 지표: Position / Velocity / Torque의 RMSE, NRMSE
- Step 비교: rise time, settling time, overshoot
- Ramp 비교: torque-velocity 관계
- Sine 비교: gain / phase
- 최종 검증: held-out S-curve에서의 일반화 성능

**발표 포인트**
- 결과 슬라이드에 들어갈 그림과 표가 왜 필요한지 먼저 정의하는 슬라이드다.

---

### 13. 결과 슬라이드 1

**제목**
- 결과 요약

**문안**
- `04_1`: 원본 해상도 기반 전체 피팅
- `04_2`: downsampling 기반 refinement 피팅
- `04_3`: 초기값 탐색 및 절차 점검
- 최종 비교는 `04_1`과 `04_2` 중심으로 정리

**넣을 후보**
- 파라미터 후보 비교 표
- 학습 로그 기준 loss 요약 표
- held-out 기준 loss 요약 표

**발표 포인트**
- `04_3`은 최종 결과 비교보다 초기 설정용 실험으로 정리한다.
- 결과 비교의 중심은 raw 기반 후보와 downsampling 기반 후보의 차이이다.

---

### 14. 결과 슬라이드 2

**제목**
- 대표 파형 비교

**문안**
- 1 ms 원본 데이터는 정보가 많지만, 현재 모델이 설명하지 못하는 고주파/비선형도 함께 포함한다.
- 반대로 샘플 수를 너무 줄이면 응답 형상, 위상, reversal timing 정보가 약해질 수 있다.
- 따라서 downsampling은 단순 축소가 아니라, 현재 모델이 설명 가능한 응답에 집중하기 위한 선택이었다.

**넣을 후보**
- `5 Hz`, `10 Hz` 대표 파형 비교 (그래프)
- reversal 확대 (방향 전환 부분) (그래프)
- held-out S-curve 확대 (그래프)

**발표 포인트**
- 그래프를 보면서 raw 1 ms와 downsampling의 trade-off를 짧게 설명한다.
- "학습에 사용할 샘플링 데이터를 고르는 것도 중요한 요소였다"는 메시지만 남긴다.

---

### 15. discussion: 현재 디지털 트윈의 유효 범위

**제목**
- 현재 디지털 트윈의 유효 범위 

**문안**
- 재현 대상: 무부하 단일 서보 모터 1축
- 재현 범위: 위치, 속도 중심의 평균 응답
- 포함 요소: 지연, 제어 응답, 토크 제한, 유효 관성, 점성/쿨롱 마찰
- 현재 범위 밖: 고주파 응답, 강한 비선형, stick-slip, deadband, 속도 추정 노이즈, 토크의 정밀 재현

**발표 포인트**
- 이 슬라이드는 한계를 말하는 슬라이드기도 하지만, 현재 모델이 어디까지 유효한지 정의하는 것에 집중한다.

---

### 16. discussion: 한계와 결과 해석

**제목**
- 결과 해석과 한계

**문안**
- torque 재현도는 position, velocity 대비 제한적이었다.
- 5 Hz, 10 Hz 구간에서는 현재 모델이 설명하지 못하는 비선형성과 노이즈 영향이 더 크게 나타났다.
- 따라서 해당 구간은 대표 비교 예시로 제시하되, 최종 피팅의 중심 데이터로는 사용하지 않았다.
- 현재 결과는 전체 시스템 복제가 아니라, 설명 가능한 범위의 1축 그레이박스 디지털 트윈으로 해석해야 한다.

**발표 포인트**
- 고주파를 뺀 이유를 임의 선택이 아니라 모델 설명 범위의 문제로 말하기
- 어디까지 맞고 어디서부터 안맞는지를 명확히 말하기
- 토크 채널은 활용했지만 재현도는 상대적으로 제한적이며, 원인 분석이 더 필요하다고 정리하기
모델이 평균 응답을 추종하나, 위상 지연 및 reversal 부근 오차 존재 
비선형성과 노이즈 영향이 더 크게 나타나며, 현재 모델의 설명력 한계가 명확히 드러남 

**슬라이드 구성**
- 좌측: 5 Hz, 10 Hz 대표 파형 비교 그림
- 우측 : 위 문안의 내용


---

### 17. 다음 단계와 개선 방향

**제목**
- 개선 방향과 다음 단계

**문안**
- 개선점
- Newton actuator/API를 우선 활용하고, API로 커버되지 않는 부분만 사용자 코드에서 직접 계산하는 hybrid 구조로 개선
- loss를 band-limited 또는 shape-centered 방식으로 개선 검토
- 토크 모델의 정교화.
- held-out 기반 검증 강화

- 향후 방향성
- 필요 시 고주파(비선형성. reversal부분) 보정 모델 별도 검토
- 단일 축 모델을 CR3A 축별 모델로 확장하기 위한 입력/출력 정의 정리
  - 순서대로 먼저 URDF 불러오고(여기까지는 함), 실제 구동을 위한 관절별 actuator와 제어 구조정의
  - 외부 부하, 중력, 링크/감속기 영향, 다축 커플링
  - 접촉에 대한 것 (deformable body 상호작용 확장)
- GPU 병렬 최적화 및 아이작랩 연계 검토

**메모**
- 제어 구조 정의란 -> 각 관절에 어떤 actuator를 둘지, 무엇을 입력으로 두고, 어떤 제어기를 둘지, 지연 토크 제한 같은 drive 응답을 어떻게 넣을지   즉! 어떻게 움직이게 할 것인가
- 위에 언급한 외부 부하, 중력, 링크/감속기 영향, 다축 커플링 -> 물리 모델, 플랜트 확장 영역임 / 로봇이 실제로 어떤 동역학을 가지는가에 대한 것.    즉! 움직일 때 어떤 물리를 받는가


---

## 발표 운영 메모

- 슬라이드 3~4: 왜 이 프로젝트를 했는가
- 슬라이드 5~8: 목표 달성을 위해 무엇을 배웠는가
- 슬라이드 9~12: 실제로 어떻게 구현하고 검증했는가
- 슬라이드 13~14: 결과 자리
- 슬라이드 15~17: discussion

## 실제 `pptx` 만들 때 우선순위

1. 텍스트보다 구조가 먼저 보이게 만들 것
2. 슬라이드 6, 9, 10, 11은 도식형 또는 시각자료 중심으로 만들 것
3. 결과 슬라이드는 그림이나 표, 그래프 빼고 일단 글 먼저. 해당 시각자료는 추후에
4. 마지막 discussion 슬라이드를 가장 중요하게 다뤄야하지 않을까...

## 이후 작업 메모

- 이 문서를 기준으로 실제 `pptx`를 생성한다.
- 실제 `pptx`는 나중에 윈도우 바탕화면 경로에 저장한다.
